{
  "name": "spmv-wasm",
  "version": "1.0.0",
  "description": "Repository to test SPMV wasm in the browsers, it iteraters throught each matrix compiling\n with emscripten, and serves the arguments to the browser",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "David Herrera",
  "license": "ISC",
  "dependencies": {
    "colors": "^1.1.2",
    "express": "^4.18.2",
    "http-server": "^0.11.2",
    "shelljs": "^0.8.0",
    "yargs": "^10.1.1"
  }
}
